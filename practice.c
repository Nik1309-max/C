#include <stdio.h>
#include <stdlib.h>
struct ListNode{
    int data;
    struct ListNode *next;
};
int main(){
    s
    struct ListNode *newNode;
    newNode = malloc(sizeof(struct ListNode));
    newNode = START;
    return 0;
}